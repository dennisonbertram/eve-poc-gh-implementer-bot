# Example issue to file on a target repo

**Title:** Implement add(a,b) in src/math.js

**Body:**
The `add(a, b)` function in `src/math.js` currently throws "not implemented".
Implement it so the tests in `test/math.test.js` pass. Then open a PR that closes this issue.
